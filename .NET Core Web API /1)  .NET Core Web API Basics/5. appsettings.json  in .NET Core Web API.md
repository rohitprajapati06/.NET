In an **ASP.NET Core Web API**, `appsettings.json` is the primary configuration file used to store application settings such as database connection strings, JWT secrets, logging configuration, SMTP settings, API keys, and custom options.

## Basic Structure

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=.;Database=SmartHealthcareDb;Trusted_Connection=True;TrustServerCertificate=True;"
  },

  "JwtSettings": {
    "Secret": "YourSuperSecretKeyHere123456789",
    "Issuer": "SmartHealthcareAPI",
    "Audience": "SmartHealthcareClient",
    "ExpiryMinutes": 60
  },

  "EmailSettings": {
    "Host": "smtp.gmail.com",
    "Port": 587,
    "Email": "example@gmail.com",
    "Password": "app-password"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
```

---

## Accessing Values

### Connection String

```csharp
builder.Services.AddDbContext<ApplicationDbContext>(options =>
    options.UseSqlServer(
        builder.Configuration.GetConnectionString("DefaultConnection")));
```

---

### JWT Settings

Create a settings class:

```csharp
public class JwtSettings
{
    public string Secret { get; set; } = string.Empty;
    public string Issuer { get; set; } = string.Empty;
    public string Audience { get; set; } = string.Empty;
    public int ExpiryMinutes { get; set; }
}
```

Register it:

```csharp
builder.Services.Configure<JwtSettings>(
    builder.Configuration.GetSection("JwtSettings"));
```

Inject it:

```csharp
public class TokenService
{
    private readonly JwtSettings _jwtSettings;

    public TokenService(IOptions<JwtSettings> options)
    {
        _jwtSettings = options.Value;
    }
}
```

---

## Environment-Specific Configuration

You can have multiple configuration files:

```text
appsettings.json
appsettings.Development.json
appsettings.Staging.json
appsettings.Production.json
```

Example `appsettings.Development.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=.;Database=SmartHealthcareDb_Dev;Trusted_Connection=True;"
  }
}
```

ASP.NET Core automatically loads:

1. `appsettings.json`
2. `appsettings.{Environment}.json`

The environment is determined by:

```text
ASPNETCORE_ENVIRONMENT=Development
```

---

## Recommended `appsettings.json` for your **Smart Healthcare Appointment & Records System**

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=.;Database=SmartHealthcareDb;Trusted_Connection=True;TrustServerCertificate=True;"
  },

  "JwtSettings": {
    "Secret": "VeryLongSecretKeyAtLeast32Characters",
    "Issuer": "SmartHealthcareAPI",
    "Audience": "SmartHealthcareUsers",
    "ExpiryMinutes": 60
  },

  "EmailSettings": {
    "Host": "smtp.gmail.com",
    "Port": 587,
    "Email": "yourmail@gmail.com",
    "Password": "your-app-password"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
```

For your project, the `appsettings.json` file should reside only in the **SmartHealthcare.API** project, since that is the startup project and all other layers consume configuration through dependency injection.
